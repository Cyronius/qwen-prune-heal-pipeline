# Rented-GPU environment for the healing run.
# Pin nothing tighter than needed, but transformers must be >= 5.14 because that is
# where Qwen3_5Moe support and the experts_implementation dispatch landed.

torch>=2.6
transformers>=5.14.1
peft>=0.20.0
accelerate>=1.0
safetensors>=0.4
numpy>=1.26
datasets>=3.0

# 4-bit base weights for QLoRA. Skip if training in plain bf16.
bitsandbytes>=0.45

# GDN fast path. Without both of these the linear-attention layers fall back to a
# slow pure-PyTorch implementation, and GDN is two thirds of this model's depth.
# CUDA only -- they will not install on the Windows CPU box.
flash-linear-attention>=0.1
causal-conv1d>=1.4
